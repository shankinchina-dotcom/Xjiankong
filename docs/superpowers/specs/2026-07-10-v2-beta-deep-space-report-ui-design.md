# v2-beta 深空研判报告 UI 重构设计

> 状态：**本地代码实现、Gate 7 免费独立审计与 Gate 8 发布候选冻结已完成；Gate 9 本地镜像验证仍未完成。Docker Engine/BuildKit 对 GHCR 与 Docker Hub 的 metadata/pull 通道均卡住；`uv` 本地 stage cache 已验证，Python base cache 首次导入失败并留下 714B 无效标签。RC 镜像尚未生成，未部署。**
>
> 本文覆盖 v2-beta 首页、历史入口和证据流的视觉与交互重构；它补充并覆盖 `2026-07-10-v2-beta-ui-visual-standards.md` 中与图标语言、卡片结构有关的旧约定。产品边界、历史数据边界、来源追溯要求仍以 `2026-07-09-v2-beta-intelligence-history-ui-design.md` 为准。
>
> 设计闸门：Gate 2.5「深空研判报告视觉重构设计」、Gate 3「原始消息链接 404 只读诊断」、Gate 4「Codex 审计 mockup 与数据映射」、Gate 5「受控 `history.json` 生成」、Gate 6「深空报告与历史 UI 本地实现」、Gate 7「本地免费独立审计」与 Gate 8「发布候选冻结」均已完成。发布候选为本地分支 `codex/v2-beta-rc`，HEAD `8f7e385a`；未构建镜像、未部署生产。

> 视觉参考：[`v2-beta-deep-space-report-reference.png`](v2-beta-deep-space-report-reference.png)。图中标题、条目、时间和版本号均为视觉占位，不是项目真实数据；连续报告、单色线性图标、深空蓝 Hero、证据流和历史抽屉才是应继承的设计要点。

## 一、目标与非目标

### 1.1 目标

将日报从「深色卡片列表」改成可连续阅读的 **深空研判报告**：用户打开首页后先得到一个明确结论，再按 8 个固定板块理解理由，最后才按需查看证据流与历史版本。

目标体验：

1. 首屏 10 秒知道当前版本、是否最新和今日一句判断。
2. 3 分钟连续读完 8 板块，不被重复卡片边框打断。
3. 需要核查时能从 `[Rxxx]` 跳到来源；链接修复前不得伪造“可回溯”。
4. 历史版本平时不占正文宽度，打开时才从右侧抽屉出现。

### 1.2 非目标

- 不改变 v2-alpha 的 8 板块名称、顺序或研判语义。
- 不增加信号强度、热度分、升温冷却、熵、簇图、昨日差分等没有上游真实数据的视觉化指标。
- 不新增图标库、前端框架或后端服务。
- 不将 RSS、热榜重新做成主页主角；它们仍是证据层。

## 二、推荐方向：深空研判报告

三个候选方向中，采用「深空研判报告」：它兼顾日报阅读、长期历史浏览和来源追溯；不采用仪表盘式“指挥台”，避免制造实时监控错觉；也不采用偏杂志的纯排版风格，避免削弱版本、来源与数据状态。

视觉关键词：**深空蓝、连续报告、低噪高密度、可追溯证据、克制发光**。

```text
顶部状态栏：产品名 / 当前版本 / 最新状态 / 历史入口
    ↓
今日总控台：一句判断 + 真实运行指标
    ↓
连续报告主体：8 个章节，而不是 8 张同质卡片
    ↓
证据流：低一级视觉权重的原始情报
    ↓
右侧历史抽屉：仅在用户主动打开时出现
```

## 三、信息层级与组件规格

### 3.1 顶部状态栏 `intel-topbar`

高度紧凑，承载全局上下文，不再与 Hero 做成两块视觉相同的大卡片。

| 元素 | 内容 | 规则 |
|---|---|---|
| 品牌 | `AI 情报终端` + 卫星线性图标 | 仅一个高权重品牌锚点 |
| 版本 | `2026-07-10 20:00` | 来自真实报告生成时间 |
| 状态 | `最新` 或 `历史版本` | 低噪 badge，不用红色 |
| 计数 | `今日 N 版` | 只在 manifest 已提供版本数时显示 |
| 操作 | `历史` 按钮 | 唯一主操作，打开抽屉 |

桌面端同行排列；窄屏下版本与状态换到第二行，历史按钮始终保留可点击面积。

### 3.2 今日总控台 `judgment-hero`

Hero 是唯一允许有明显蓝色能量渐变的区块。它不做仪表盘，而是报告封面：

1. eyebrow：`AI DAILY INTELLIGENCE / 当前版`，小字号等宽字。
2. 主标题：`今日一句判断`。
3. 结论：最多 3 行，优先呈现 `daily_judgment`；为空时显示真实空态，不填示例文案。
4. 指标行：热榜命中、RSS 命中、AI 8 板块、生成时间、今日版本数。所有值必须来自现有输出或未来 `history.json`。

Hero 不放大数字、不加动态图表、不把采集成功率包装成业务“评分”。

### 3.3 连续报告主体 `report-shell`

8 板块装入一个报告容器。容器本身使用一层弱边界与轻投影，章节之间用发光程度很低的分隔线和留白区分，避免出现 8 张“同一款卡片”的疲劳感。

每章 `report-section` 固定为：

```text
02  [line-icon] 研究／模型突破
    一句引导总结
    ┃ 判断条目；[R002] [R003]
    ┃ 判断条目；[R004]
```

- 两位编号是阅读锚点；编号色使用强调蓝，不用彩色状态含义。
- 左侧 `section-rail` 是 2px 蓝色细光轨；当前章节 hover 时仅提亮边框和轨道。
- `sec-summary` 是低饱和次级文字，来自 AI 生成结果；缺失时不显示空框。
- 条目为连续列表，单条不再套独立卡片；内容多时维持 4–6 条，不因视觉缩成两三条。
- 08 原始来源索引按来源类别分组，统计行只用真实可得数量。

### 3.4 来源引用与 404 保护 `source-ref`

`[Rxxx]` 是可点击的来源标记，而不是普通正文。正式接入前必须先完成“原始消息链接 404”只读诊断。

| 状态 | 呈现 | 行为 |
|---|---|---|
| 已验证 canonical URL | 蓝灰边框小标记 | 新窗口打开原始来源 |
| X/Nitter 有可用 fallback | 标记后展示来源名 | 弹出或展开原文 / 镜像两个明确入口 |
| 无可用链接或诊断未完成 | 中性不可点击文字 | 不显示伪链接，不将用户送往已知 404 |

AI 不生成 URL；链接只来自 SourceRef、RSS 原始 canonical URL 或经 Gate 3 验证的 URL 转换规则。

### 3.5 证据流 `evidence-stream`

证据流置于报告主体之后，视觉比研判正文弱一级：深色平面条目、低饱和细边、无厚阴影。

- 每条仅显示来源、时间、低噪 `NEW` badge 和标题。
- 标题三行截断；鼠标悬停只提升边框亮度。
- PC 使用 2 列自适应网格；移动端单列。
- `NEW` 使用琥珀或浅蓝胶囊，不能用纯红文字或无解释的红色数字。

### 3.6 历史抽屉 `history-drawer`

历史不常驻，不压缩报告正文。点击顶部历史按钮后从右侧滑出，约 `280ms`：

```text
当前版本（日期 / 时间 / 最新）
今日时间线（倒序多版）
近期（按日显示版本数与最近时间）
历史归档（可滚动）
```

关闭支持遮罩、关闭按钮和 `ESC`。打开历史版本时，正文顶部必须出现“当前查看：某时刻版本；这不是最新版本；查看最新”的可见提示。

## 四、视觉语言

### 4.1 色彩 token

| token | 值 | 用途 |
|---|---|---|
| `--bg-base` | `#07111f` | 深空底色 |
| `--bg-field` | `#0c1c33` | 顶部径向信号场 |
| `--surface-report` | `rgba(17, 39, 70, .82)` | 连续报告主体 |
| `--surface-evidence` | `rgba(12, 29, 52, .76)` | 证据流 |
| `--surface-focus` | `linear-gradient(135deg, rgba(27, 86, 166, .58), rgba(21, 52, 106, .82))` | Hero / 当前版本 |
| `--line-subtle` | `rgba(153, 190, 245, .18)` | 常规边界 |
| `--line-active` | `rgba(111, 178, 255, .55)` | hover / 当前状态 |
| `--text-primary` | `#edf5ff` | 主文字 |
| `--text-secondary` | `#9eb4d3` | 摘要、元信息 |
| `--accent-blue` | `#75b2ff` | 编号、链接、图标 |
| `--accent-cyan` | `#a5d4ff` | 小范围高亮 |

背景使用一个低对比径向场，而不是滚动变色动画。亮区只服务于 Hero、当前历史版本和 hover，不让全页都发光。

### 4.2 图标系统

取消彩色 emoji，改为本地内联 SVG 线性图标：`viewBox="0 0 24 24"`、`stroke="currentColor"`、`stroke-width="1.7"`，默认 `16px`，Hero 可用 `20px`。

不引入第三方图标依赖。图标只传达分区语义，标题文字仍是信息主角。

| 模块 | icon id |
|---|---|
| 今日核心判断 | `crosshair` |
| 研究／模型突破 | `flask` |
| 产品与工具变化 | `wrench` |
| 开源项目与开发者生态 | `network` |
| 大厂／融资／政策／算力动态 | `cpu` |
| X 关键人物观点 | `message-circle` |
| 风险与下周关注 | `triangle-alert` |
| 原始来源索引 | `book-open` |
| 证据流 / 顶部品牌 | `satellite` |
| 历史 | `history` |

### 4.3 排版与密度

- 字体：系统字体栈，正文优先 `PingFang SC`；数字和时间可用系统等宽字体。
- 正文行高 `1.7` 左右；报告正文最大行宽 `760–820px`，避免大屏一行过长。
- 页面容器最大 `1180px`；报告主体在桌面端保留足够左右留白。
- 大标题 28–32px；章节标题 18–20px；摘要 13–14px；来源标记 12px。
- 阴影仅使用 `0 10px 30px rgba(0, 0, 0, .16)` 以下的轻量级别。

## 五、响应式与可用性

| 视口 | 规则 |
|---|---|
| `≥ 1024px` | 报告全宽；证据流两列；历史抽屉 380px |
| `640–1023px` | 内容左右留白收缩；证据流仍可两列；抽屉最大 88vw |
| `< 640px` | 顶栏换行；Hero 指标自动折行；报告章节缩小内边距；证据流单列；抽屉占 92vw |

- 所有按钮和引用链接需要明确 hover/focus 状态；键盘 `ESC` 必须关闭抽屉。
- 颜色不是唯一状态表达：最新/历史/不可点击来源必须有文字。
- 长标题必须截断而非撑破容器；来源统计与空态必须可读。

## 六、数据与空态边界

| 数据缺失 | 页面行为 |
|---|---|
| 没有 `daily_judgment` | Hero 显示“本版未生成 AI 研判”，不填 mock 文案 |
| 某研判板块为空 | 保留标题与简短“暂无高置信信号”说明 |
| 尚无 `history.json` | 历史按钮可隐藏或显示“历史归档准备中”；不可展示硬编码历史时间线 |
| 来源 URL 未验证 | 展示来源 ID，不输出可点击链接 |
| RSS 为 0 | 证据流显示真实空态，不显示示例卡片 |

## 七、实现边界与验证要求

实现必须按既定闸门推进，当前不会直接进入代码：

1. **Gate 3：原始消息链接 404 只读诊断**，已完成；详见主策划的 Gate 3 审计结果。
2. **Gate 4：Codex 审计 mockup 与数据映射**，已完成；确认核心报告内容可复用，但 `history.json`、来源状态与动态板块摘要尚无现成数据。
3. **Gate 5：history.json 生成**，已完成；从现有 HTML 快照派生受控公开索引，不包含 `.env`、DB、config、`source_map` 或任何 URL 可用性探测结果。
4. **Gate 6：前端实现**，已由临时执行 Agent 在隔离分支完成网页专用 HTML/CSS、历史抽屉、`history.html` 与来源展示规则；邮件、飞书、钉钉、Telegram 调用路径未改。
5. **Gate 7：本地免费验证**，已由 Codex 于 2026-07-11 完成自动化回归、静态审计和本地浏览器验收；不得跳至生产同步。

实现前需确认相邻 TrendRadar fork 的工作树状态；不覆盖已有改动。生产同步、镜像构建、NAS 上传、容器重启、付费 AI 验证均另起闸门并由老板单独确认。

## 八、Gate 6 本地实现记录（2026-07-10）

代码只存在于隔离 worktree `/Users/shankluo/AI/Claude/TrendRadar-v2-beta-history` 的 `codex/v2-beta-history` 分支，提交顺序如下：

- `c82cc6bb`：网页专用深空研判渲染器。
- `e3e08f48`：深空报告状态范围修正。
- `37bed9d6`：历史抽屉与独立 `history.html`。
- `4d0bc1c4`：快照上下文传递修正。
- `8f274cce`：公开历史快照路径校验。
- `ad0be46a`：Nitter 状态 URL 的确定性 `x.com` fallback。
- `8b68e580`：manifest 驱动的版本状态、正文旧版提示与 RSS 深空证据流。
- `0986303f`：旧版提示移至报告顶栏与 Hero 之间。
- `a8d64784`：AI 失败或跳过时保留历史上下文外壳。
- `8f7e385a`：隐藏深空页旧搜索框，并修正最新页隐藏提示的计算样式。

本地临时目录实际生成了 `output/index.html`、时间戳快照、`output/history.json`、`output/history.html` 与 `output/html/latest/daily.html`，并验证了 8 个章节、`data-snapshot-path`、历史抽屉/`ESC`、历史页、Nitter → X fallback、HTML 转义和拒绝 `javascript:` 链接。Gate 7 自动化结果为：v2-beta 深空网页 15/15、history manifest 2/2、v2-alpha schema、解析 8/8、HTML 渲染 10/10；相关模块编译和差异检查通过。

Codex 又通过 `127.0.0.1` 本地预览检查了实际计算样式：顶栏显示“最新”和“今日 1 版”，最新页的旧版提示为不可见，旧搜索框为不可见，RSS 证据流为低饱和深蓝背景。该浏览器检查未访问生产站点。

来源 fallback 只依据 Nitter 状态 URL 的确定性转换规则生成，不进行运行时外部可用性探测，也不声称 canonical 或 X 页面当时可访问。未知、缺失或非 `http/https` URL 继续保持非链接文本。

尚未执行：合并到 `v2-alpha`、镜像构建、上传 NAS、修改 `.env`、容器重建或重启、付费 AI 调用、Cloudflare 修改、生产部署。外部 canonical URL 仍可能因源站删除、登录或地区限制失效；本轮不进行运行时可用性探测。

## 九、Gate 8 发布候选记录（2026-07-11）

- 发布候选分支：`codex/v2-beta-rc`。
- 发布候选 HEAD：`8f7e385ac1453521a7ffffa9c5de43d725af76b9`。
- 生产基线：`v2-alpha` HEAD `33d80973`；RC 与基线为严格快进关系，领先 12 个提交、落后 0 个提交。
- 差异范围：新增 `v2_beta_deep_space_ui.py`、`v2_beta_history_manifest.py`；修改 `formatter.py`、`context.py`、`generator.py`、`html.py`。没有 config、Docker、数据库或凭据文件差异。
- `docker/Dockerfile` 第 63 行使用 `COPY trendradar/ ./trendradar/`，后续构建会包含四个运行模块；本 Gate 仅做静态核对，没有执行 Docker build。
- 主 `v2-alpha` worktree 保留既有未提交改动，因此没有直接 merge；后续生产镜像应直接以 RC 分支作为输入，避免覆盖主 worktree。

下一步为单独的 Gate 9「本地 RC 镜像构建与容器级免费验证」。该闸门仍不上传 NAS、不修改 `.env`、不重启生产容器；生产同步必须继续单独审批。

## 十、Gate 9 本地镜像构建阻塞记录（2026-07-11）

- 构建输入仍为冻结 RC：`codex/v2-beta-rc` / `8f7e385ac1453521a7ffffa9c5de43d725af76b9`，未发生代码漂移。
- Docker Client / Server 均为 `29.5.3`，构建文件和 RC 差异范围核验通过。
- 两次执行构建均在读取 `ghcr.io/astral-sh/uv:latest` metadata 时超时；随后 `--platform linux/amd64 --pull=false` 构建命中本地 `uv` cache，却在读取 `docker.io/library/python:3.12-slim-bookworm` metadata 时同样超时；构建未进入 RC 源码层。
- 宿主机与临时容器均可访问 GHCR / Docker Hub 的公开 registry API；卡点限定为 Docker Engine/BuildKit 的 metadata/pull 通道，而不是 RC 源码或公开 registry 整体不可达。
- 已从 v2-alpha 镜像提取并校验仅含 `/uv`、`/uvx` 的 `linux/amd64` 本地 stage cache（`uv 0.11.22`）；Python 官方 base cache 的首次导入失败，仅留下 714B 无效 `python:3.12-slim-bookworm` 标签，不能参与构建。
- 未生成 `xjiankong-trendradar:v2-beta-rc-20260711`，因此没有执行 image inspect 或一次性容器验证。
- 未修改 Dockerfile、Docker Desktop 设置、镜像源、`.env` 或生产环境；未访问 NAS、未上传、未重启容器、未调用付费 AI。

Gate 9 当前停在受控恢复点：先仅清理本次任务产生的 714B 无效 Python 标签，再重建并验证官方 `linux/amd64` Python base cache；成功后只重试一次本地 build 和一次性容器验证。不得修改 Dockerfile、Docker Desktop 设置、NAS、`.env` 或生产环境；镜像构建成功和容器级免费验证完成前，不得进入镜像上传或 NAS 同步。

## 十一、验收标准

- [x] 页面首屏清楚区分状态栏、今日结论、真实指标；没有两块相似的大卡片。
- [x] 8 板块视觉上是一份连续报告，不是同质卡片墙。
- [x] 深空报告使用统一单色线性 SVG 图标，不使用彩色 emoji 作为系统图标。
- [x] Hero、当前版本与普通区域存在清晰但克制的蓝色层级。
- [x] RSS 作为证据层，视觉权重低于研判正文。
- [x] 历史抽屉默认零占用，支持遮罩、关闭按钮和 `ESC`。
- [x] 所有数值、时间与版本均来自实际报告或 manifest；缺失时显示真实空态。
- [x] Nitter 状态来源提供确定性 X fallback；无 URL 或非 `http/https` 不生成链接。

## 十一、交接给执行 Agent 的一句话

做的是一份可连续阅读的深空研判报告，不是一组发光卡片：**结论优先、报告连续、证据退后、历史按需出现、每一个数字和链接都必须真实。**
